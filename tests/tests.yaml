blueprint:
  name: Action Selector Test
  description: A test to confirm that action selector shows full action editor
  domain: automation
  input:
    test_action:
      name: Test Action
      description: Full action input
      default: []
      selector:
        action: {}

trigger:
  - platform: time
    at: "12:00:00"

action:
  - choose:
      - conditions: []
        sequence: !input test_action
